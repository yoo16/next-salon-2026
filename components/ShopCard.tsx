// rfc スニペットを使う
// Shop型ファイルをインポート
import { Shop } from '@/types/shop'

type ShopCardProps = {
    shop: Shop
}

export default function ShopCard({ shop } : ShopCardProps) {
  return (
    <div>
      
    </div>
  )
}